import type{APIRoute}from'astro';import{createCmsClient}from'../../../../lib/cms/supabase';export const POST:APIRoute=async({request,cookies,params,redirect,locals})=>{if(!locals.cmsUser)return new Response('Unauthorized',{status:401});const f=await request.formData();const pos=Math.max(1,Number(f.get('author_position'))||1),count=Math.max(pos,Number(f.get('author_count'))||1);const p={title:String(f.get('title')??'').trim(),status:String(f.get('status')),abstract:String(f.get('abstract')??'').trim(),publication_url:String(f.get('publication_url')??'').trim()||null,venue:String(f.get('venue')??'').trim(),publisher:String(f.get('publisher')),publication_year:Number(f.get('publication_year')),doi:String(f.get('doi')??'').trim(),authorship_label:String(f.get('authorship_label')),author_position:pos,author_count:count,sort_order:Math.max(0,Number(f.get('sort_order'))||0),visible:f.get('visible')==='true'};if(p.title.length<3)return new Response('Invalid paper',{status:400});const s=createCmsClient(request,cookies)!;const q=params.id==='new'?s.from('research_papers').insert(p):s.from('research_papers').update(p).eq('id',params.id);const{error}=await q;if(error)return new Response(error.message,{status:400});return redirect('/admin/research')};
